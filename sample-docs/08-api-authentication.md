# API Authentication

Every API request requires authentication.

## Secret Keys

Use your secret API key only on the server.

## Publishable Keys

Publishable keys are safe to expose in frontend applications.

## Authorization Header

Send the API key using the Authorization header as a Bearer token.
